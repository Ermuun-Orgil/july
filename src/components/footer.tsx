import { Box, Typography } from "@mui/material"

export const Footer = () => {
    return (
        <Box borderTop={1} position={"absolute"} bottom={0} width={"100vw"}>
            <Typography>footer</Typography>
        </Box>
    )
}